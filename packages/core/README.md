# @rd/core

**Owner (per [Masterfile v2.4](../../docs/MASTERFILE.md) §4):** Standards & foundational types.

Placeholder package — reserves the `packages/core` slot in the target repo
layout ([Masterfile §3](../../docs/MASTERFILE.md#3-repository-structure)).
No implementation yet: nothing currently justifies extracting foundational
types/config out of [`apps/nexus`](../../apps/nexus) into a shared package,
since there is only one consumer. Populate this once a second app/package
needs the same standards.
